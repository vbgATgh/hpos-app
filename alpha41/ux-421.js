/* HPOS Alpha 4.2.1 – Decision UX
   Verdichtet Home/Navigation, macht Income lesbarer und strukturiert Analyse/Halal-Verifikation.
   Keine erfundenen Markt- oder Halal-Daten. */
(() => {
'use strict';
const MARKET421='../data/market/market_summary.json';
const ROOT421=[['dashboard','⌂','Home'],['portfolio','◫','Portfolio'],['radar','◎','Radar'],['income','€','Income'],['strategy','⚙','Strategie']];
let market421=null,market421Loading=null;

function ui421(){state.userProfile.ui=state.userProfile.ui||{};state.userProfile.ui.alpha421=state.userProfile.ui.alpha421||{};return state.userProfile.ui.alpha421;}
function parent421(){if(['watchlist','news','halal'].includes(currentPage))return'radar';if(['transactions','asset'].includes(currentPage))return'portfolio';if(['data','diagnostics','more'].includes(currentPage))return'strategy';return currentPage;}
function norm421(v){return String(v??'').toUpperCase().replace(/[^A-Z0-9]/g,'');}
function css421(){if($('#ux421css'))return;const s=document.createElement('style');s.id='ux421css';s.textContent=`
.home-hero421{padding:18px}.home-value421{font-size:31px;font-weight:820;letter-spacing:-.025em}.home-sub421{color:var(--muted);font-size:12px;margin-top:4px}.kpi421{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:10px}.kpi421 button,.kpi421>div{border:1px solid var(--line);background:rgba(255,255,255,.025);color:var(--text);border-radius:13px;padding:11px;text-align:left}.kpi421 small{display:block;color:var(--muted);font-size:11px}.kpi421 strong{display:block;font-size:18px;margin-top:4px}
.visual-grid421{display:grid;gap:10px}.viz421{padding:14px}.viz421 h3{margin:0;font-size:15px}.viz421 .sub421{color:var(--muted);font-size:11px;margin-top:3px}.stackbar421{height:12px;border-radius:99px;overflow:hidden;display:flex;background:var(--line);margin:12px 0 8px}.stackbar421 span{height:100%}.mini-bars421{height:94px;display:flex;align-items:flex-end;gap:5px;margin-top:10px}.mini-bars421 span{flex:1;min-width:5px;background:var(--green);border-radius:5px 5px 2px 2px;opacity:.88}.top-pos421{display:grid;gap:8px;margin-top:11px}.top-pos421 button{border:0;background:transparent;color:var(--text);padding:0;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;text-align:left}.top-pos421 small{display:block;color:var(--muted);font-size:10px;margin-top:2px}
.action-grid421{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.action-grid421 button{border:1px solid var(--line);background:var(--panel);color:var(--text);border-radius:13px;padding:11px 8px;text-align:center}.action-grid421 strong{display:block;font-size:21px}.action-grid421 small{display:block;color:var(--muted);font-size:10px;margin-top:3px}.action-grid421 .bad421 strong{color:var(--red)}.action-grid421 .warn421 strong{color:var(--orange)}.action-grid421 .good421 strong{color:var(--green)}
.data-mini421{display:flex;gap:6px;overflow:auto;scrollbar-width:none}.data-mini421 button{white-space:nowrap;border:1px solid var(--line);background:var(--panel);color:var(--muted);border-radius:999px;padding:7px 10px;font-size:11px}.data-mini421 .CURRENT{color:var(--green)}.data-mini421 .STALE{color:var(--orange)}.data-mini421 .ERROR{color:var(--red)}
.income-selected421{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 12px;border-radius:12px;background:rgba(56,217,150,.07);border:1px solid rgba(56,217,150,.25);margin:10px 0}.income-selected421 strong{font-size:18px}.month-btn421{width:100%;height:100%;border:0;background:transparent;color:inherit;padding:0;display:grid;grid-template-rows:1fr 20px;align-items:end}.month-btn421 .bar-wrap{height:150px}.month-col.sel421{outline:1px solid var(--green);outline-offset:2px;border-radius:7px}
.decision421{padding:15px}.decision-head421{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.decision-state421{font-size:24px;font-weight:820}.decision-state421.OK{color:var(--green)}.decision-state421.REVIEW{color:var(--orange)}.decision-state421.CRITICAL{color:var(--red)}.decision-reasons421{display:grid;gap:7px;margin-top:12px}.decision-reasons421 div{display:flex;gap:8px;align-items:flex-start;color:var(--muted);font-size:12px}.quality421{height:8px;background:var(--line);border-radius:99px;overflow:hidden;margin-top:7px}.quality421 span{display:block;height:100%;background:var(--blue)}
.price-eur421{margin-top:6px;font-size:13px;color:var(--green)}.price-local421{font-size:11px;color:var(--muted);margin-top:2px}.verify421{border-left:4px solid var(--blue)}.verify421 .steps421{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:10px}.verify421 .steps421 div{border:1px solid var(--line);border-radius:11px;padding:9px}.verify421 small{display:block;color:var(--muted);font-size:10px}.verify421 strong{display:block;margin-top:3px;font-size:13px}
.manage421{display:grid;gap:10px}.manage-group421{padding:0;overflow:hidden}.manage-title421{padding:13px 14px 8px;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.04em}.manage-row421{width:100%;border:0;border-top:1px solid var(--line);background:transparent;color:var(--text);padding:13px 14px;display:grid;grid-template-columns:32px minmax(0,1fr) auto;gap:10px;align-items:center;text-align:left}.manage-row421 span:nth-child(2) small{display:block;color:var(--muted);font-size:11px;margin-top:3px}
@media(min-width:760px){.visual-grid421{grid-template-columns:1fr 1fr}.kpi421{grid-template-columns:repeat(4,1fr)}}
`;document.head.appendChild(s);}

async function loadMarket421(force=false){if(market421&&!force)return market421;if(market421Loading&&!force)return market421Loading;market421Loading=fetch(`${MARKET421}?v=${force?Date.now():'421'}`,{cache:'no-cache'}).then(r=>r.ok?r.json():Promise.reject()).then(x=>market421=x).catch(()=>market421={state:'UNKNOWN',assetIndex:{},assets:{}}).finally(()=>market421Loading=null);return market421Loading;}
function marketKey421(a){if(!a||!market421)return null;for(const[k,m]of Object.entries(market421.assetIndex||{})){const remote=[m.name,m.symbol,m.isin,...(m.aliases||[])].map(norm421),local=[a.name,a.ticker,a.isin].map(norm421);if(remote.some(x=>x&&local.includes(x)))return k;}return null;}
function marketMeta421(a){const k=marketKey421(a);return k?market421?.assets?.[k]||null:null;}
function fmt421(v,c='EUR'){try{return new Intl.NumberFormat('de-DE',{style:'currency',currency:c,maximumFractionDigits:2}).format(v);}catch{return Number(v).toLocaleString('de-DE',{maximumFractionDigits:2});}}

renderNav=function(){const active=alerts.filter(a=>!['DONE','SNOOZED'].includes(a.status)).length,p=parent421();$('#nav').innerHTML=ROOT421.map(([id,ic,l])=>`<button class="nav-btn ${p===id?'active':''}" data-route="${id}"><span>${ic}</span><span>${l}</span>${id==='radar'&&active?`<span class="nav-count">${active}</span>`:''}</button>`).join('');$$('.nav-btn').forEach(b=>b.onclick=()=>route(b.dataset.route));let e=$('#bottom42');if(!e){e=document.createElement('nav');e.id='bottom42';e.className='bottom42';document.body.appendChild(e);}e.innerHTML=ROOT421.map(([id,ic,l])=>`<button class="${p===id?'active':''}" data-r421="${id}"><i>${ic}</i><span>${l}</span>${id==='radar'&&active?`<span class="nav-count" style="position:absolute;top:3px;right:22%">${active}</span>`:''}</button>`).join('');$$('[data-r421]',e).forEach(b=>b.onclick=()=>route(b.dataset.r421));};
TITLES.strategy=['Strategie','Ziele, Regeln & System'];TITLES.more=['Strategie','Ziele, Regeln & System'];

function halalCounts421(){const sum=buildHalalPortfolioSummary(state,snapshot);return {sum,unknown:sum.unknownCount||0,h2:sum.statuses?.find(x=>x.status==='H2')?.count||0,h0:sum.statuses?.find(x=>x.status==='H0')?.count||0};}
function topPositions421(){return buildIntegratedAssetViews(state,snapshot,alerts).filter(v=>v.weightPct!==null).sort((a,b)=>Number(b.weightPct)-Number(a.weightPct)).slice(0,3);}
function incomeBars421(inc){const mx=Math.max(1,...inc.monthly);return `<div class="mini-bars421">${inc.monthly.map(v=>`<span style="height:${Math.max(4,Number(v)/mx*100)}%"></span>`).join('')}</div>`;}
function halalBar421(sum){const colors={H1:'#38d996',H2:'#ffb45c',H0:'#ff6b72',UNKNOWN:'#70a7ff'};return `<div class="stackbar421">${sum.statuses.map(x=>Number(x.pct)>0?`<span style="width:${x.pct}%;background:${colors[x.status]}"></span>`:'').join('')}</div>`;}
function renderDashboard421(){recompute();const y=new Date().getFullYear(),inc=buildIncomeSummary(state,snapshot,{year:y}),hc=halalCounts421(),active=alerts.filter(a=>!['DONE','SNOOZED'].includes(a.status)),crit=active.filter(a=>a.severity==='CRITICAL').length,warn=active.filter(a=>a.severity==='WARN').length,review=(state.reviews||[]).filter(r=>r.status!=='DONE').length,top=topPositions421();$('#main').innerHTML=`<div class="section-head"><div><h1>Home</h1><p>${new Intl.DateTimeFormat('de-DE',{weekday:'long',day:'2-digit',month:'long'}).format(new Date())}</p></div><button class="btn small" id="homeRefresh421">Aktualisieren</button></div><div class="card home-hero421"><small class="home-sub421">Depotwert</small><div class="home-value421">${snapshot.positions.length?eur(snapshot.totals.holdingsValue):'—'}</div><div class="kpi421"><button data-home421="income"><small>Income ${y}</small><strong>${eur(inc.received)}</strong></button><button data-home421="radar"><small>Offene Punkte</small><strong>${active.length}</strong></button><button data-home421="halal"><small>Halal geklärt</small><strong>${hc.sum.knownPct===null?'—':pct(hc.sum.knownPct)}</strong></button><button data-home421="portfolio"><small>Positionen</small><strong>${snapshot.positions.length}</strong></button></div></div><div class="visual-grid421" style="margin-top:12px"><div class="card viz421"><h3>Top Positionen</h3><div class="sub421">Gewichtung im Depot</div><div class="top-pos421">${top.length?top.map(v=>`<button data-home-asset421="${esc(v.position.assetId)}"><span><strong>${esc(v.position.name)}</strong><small>${eur(v.position.marketValue)}</small></span><strong>${pct(v.weightPct)}</strong></button>`).join(''):'<div class="empty">Keine bewertbaren Positionen.</div>'}</div></div><div class="card viz421"><h3>Halal-Verteilung</h3><div class="sub421">Effektiver Status, nach Depotwert</div>${hc.sum.basisValue?halalBar421(hc.sum):'<div class="empty">Noch keine Basis.</div>'}<div class="asset-badges"><span class="badge good">H1 ${hc.sum.statuses?.find(x=>x.status==='H1')?.count||0}</span><span class="badge warn">H2 ${hc.h2}</span><span class="badge bad">H0 ${hc.h0}</span><span class="badge info">Offen ${hc.unknown}</span></div></div><div class="card viz421"><h3>Income-Verlauf</h3><div class="sub421">Monatliche Ausschüttungen ${y}</div>${incomeBars421(inc)}</div><div class="card viz421"><h3>Handlungsbedarf</h3><div class="sub421">Nur aktive HPOS-Signale</div><div class="action-grid421" style="margin-top:11px"><button class="bad421" data-action421="CRITICAL"><strong>${crit}</strong><small>Kritisch</small></button><button class="warn421" data-action421="WARN"><strong>${warn}</strong><small>Prüfen</small></button><button data-action421="REVIEWS"><strong>${review}</strong><small>Reviews</small></button></div></div></div><div class="section-head"><div><h2>Datenfrische</h2></div></div><div class="data-mini421">${['portfolio','prices','halal','news'].map(k=>{const d=state.dataStatus[k]||{state:'UNKNOWN'};return`<button class="${esc(d.state)}" data-ds421="${k}">${esc(dataLabel(k))} · ${esc(friendlyDataState(d.state))}</button>`}).join('')}</div>`;$('#homeRefresh421').onclick=()=>{recompute();renderDashboard421();toast('Lokale Regeln neu geprüft');};$$('[data-home421]').forEach(b=>b.onclick=()=>route(b.dataset.home421));$$('[data-home-asset421]').forEach(b=>b.onclick=()=>openAsset(b.dataset.homeAsset421));$$('[data-action421]').forEach(b=>b.onclick=()=>{route('radar');setTimeout(()=>{const sev=b.dataset.action421;if(sev==='CRITICAL'||sev==='WARN'){$(`[data-rsev42="${sev}"]`)?.click();}},0);});$$('[data-ds421]').forEach(b=>b.onclick=()=>openDataStatus(b.dataset.ds421));}
renderDashboard=renderDashboard421;

const portfolio421=renderPortfolio;renderPortfolio=function(){portfolio421();const head=$('#main .section-head');if(head&&!$('#portfolioTx421')){const box=document.createElement('div');box.className='action-row';box.style.margin='0';box.innerHTML='<button class="btn small" id="portfolioTx421">Transaktionen</button><button class="btn primary small" id="portfolioAdd421">＋ Erfassen</button>';const old=$('#portfolioAdd');old?.remove();head.appendChild(box);$('#portfolioTx421').onclick=()=>route('transactions');$('#portfolioAdd421').onclick=()=>openActionSheet(['OPENING_POSITION','BUY','SELL']);}renderNav();};

const radar421=renderRadar;renderRadar=function(){radar421();const hub=$('.hub42');if(hub&&!$('[data-halal-hub421]')){const h=halalCounts421(),b=document.createElement('button');b.dataset.halalHub421='1';b.innerHTML=`<strong>${h.unknown+h.h2+h.h0}</strong><small>Halal prüfen</small>`;b.onclick=()=>route('halal');hub.appendChild(b);}renderNav();};

renderIncome=function(){const u=ui421(),y=new Date().getFullYear(),inc=buildIncomeSummary(state,snapshot,{year:y}),months=['Jan','Feb','Mär','Apr','Mai','Jun','Jul','Aug','Sep','Okt','Nov','Dez'];if(u.incomeMonth===undefined)u.incomeMonth=null;const sel=u.incomeMonth,max=Math.max(1,...inc.monthly),selectedValue=sel===null?null:inc.monthly[sel],tx=(state.transactions||[]).filter(t=>t.type==='DIVIDEND'&&String(t.date||'').startsWith(String(y))&&(sel===null||new Date(`${t.date}T00:00:00`).getMonth()===sel)).sort((a,b)=>String(b.date).localeCompare(String(a.date)));$('#main').innerHTML=`<div class="section-head"><div><h1>Income</h1><p>Ausschüttungen und Zielpfad</p></div><button class="btn primary small" id="divAdd">＋ Dividende</button></div><div class="grid metrics">${metric(`${y} erhalten`,eur(inc.received),`${inc.count} Ausschüttung${inc.count===1?'':'en'}`)}${metric('Ø / Monat',eur(inc.monthlyAverage),'YTD')}${metric('Zielgrad',inc.goalProgressPct===null?'—':pct(inc.goalProgressPct),`Ziel ${eur(inc.goal)}`)}${metric('Zieldatum',monthFmt(snapshot.effectiveProfile.rules.dividendGoalDate.effectiveValue),'Strategie')}</div><div class="card" style="margin-top:12px"><div class="top"><div><h3>Monatsentwicklung ${y}</h3><p>Monat antippen für Details</p></div>${sel!==null?'<button class="btn small" id="incomeAll421">Alle</button>':''}</div>${sel!==null?`<div class="income-selected421"><span><small>${months[sel]}</small><strong>${eur(selectedValue)}</strong></span><span class="badge good">${tx.length} Ausschüttung${tx.length===1?'':'en'}</span></div>`:''}<div class="month-chart">${inc.monthly.map((v,i)=>`<div class="month-col ${sel===i?'sel421':''}"><button class="month-btn421" data-income421="${i}" aria-label="${months[i]} ${eur(v)}"><div class="bar-wrap"><span style="height:${Math.max(3,v/max*100)}%"></span></div><small>${months[i]}</small></button></div>`).join('')}</div></div><div class="section-head"><div><h2>${sel===null?'Ausschüttungen':'Ausschüttungen · '+months[sel]}</h2><p>${tx.length} Einträge</p></div></div><div class="card">${tx.length?tx.map(t=>{const a=state.assets.find(x=>x.assetId===t.assetId);return`<button class="distribution-row" ${a?`data-income-asset421="${esc(a.assetId)}"`:''}><div><strong>${esc(a?.name||'Ohne Zuordnung')}</strong><small>${esc(dateFmt(t.date))}</small></div><span>${eur(t.amount)}</span></button>`}).join(''):'<div class="empty">Keine Ausschüttungen in der Auswahl.</div>'}</div><div class="section-head"><div><h2>Verteilung</h2></div></div><div class="card">${inc.distribution.length?inc.distribution.slice(0,10).map(d=>`<button class="distribution-row" ${d.assetId!=='UNASSIGNED'?`data-income-asset421="${esc(d.assetId)}"`:''}><div><strong>${esc(d.name)}</strong><small>${pct(d.sharePct)}</small></div><span>${eur(d.amount)}</span></button>`).join(''):'<div class="empty">Noch keine Dividenden.</div>'}</div>`;$('#divAdd').onclick=()=>openEntryModal('DIVIDEND');$$('[data-income421]').forEach(b=>b.onclick=()=>{u.incomeMonth=Number(b.dataset.income421);persist();renderIncome();});$('#incomeAll421')?.addEventListener('click',()=>{u.incomeMonth=null;persist();renderIncome();});$$('[data-income-asset421]').forEach(b=>b.onclick=()=>openAsset(b.dataset.incomeAsset421,'activities'));};

function decision421(v){const h=v.halal,critical=v.activeAlerts.some(a=>a.severity==='CRITICAL'),warn=v.activeAlerts.some(a=>a.severity==='WARN');let state421='OK',action='Beobachten';const reasons=[];if(h.status==='H0'||critical){state421='CRITICAL';action=h.status==='H0'?'Exit-/Veto-Review':'Kritischen Hinweis prüfen';}else if(h.status==='H2'||h.status==='UNKNOWN'||warn){state421='REVIEW';action=h.status==='H2'?'Nicht aufstocken · Review':h.status==='UNKNOWN'?'Halal verifizieren':'Hinweise prüfen';}if(h.status==='H0')reasons.push('Halal H0 aktiviert VETO.');else if(h.status==='H2')reasons.push('Halal H2 aktiviert FREEZE.');else if(h.status==='UNKNOWN')reasons.push('Halal-Status ist noch nicht geklärt.');else reasons.push('Halal-Status ist effektiv H1.');if(v.activeAlerts.length)reasons.push(`${v.activeAlerts.length} aktive HPOS-Regelmeldung${v.activeAlerts.length===1?'':'en'}.`);else reasons.push('Keine aktive HPOS-Regelmeldung zum Asset.');const mm=marketMeta421(v.asset),news=state.dataStatus.news||{state:'UNKNOWN'};if(mm?.state==='CURRENT')reasons.push('Externer Kurs-Snapshot vorhanden.');else reasons.push('Externer Kurs-Snapshot fehlt oder ist nicht aktuell.');if(news.state!=='CURRENT')reasons.push(`News-Datenstatus ${news.state}.`);const checks=[h.status!=='UNKNOWN',!v.position||Number.isFinite(Number(v.position.valuationPrice)),mm?.state==='CURRENT',news.state==='CURRENT'];const quality=Math.round(checks.filter(Boolean).length/checks.length*100);return {state:state421,action,reasons,quality,market:mm};}
const tab421=renderAssetTab;renderAssetTab=function(v,tab){if(tab==='analysis'){const d=decision421(v),last=ui421().analysisAt?.[v.asset.assetId];return`<div class="card decision421"><div class="decision-head421"><div><small class="home-sub421">HPOS Entscheidungsstatus</small><div class="decision-state421 ${d.state}">${d.state==='OK'?'OK':d.state==='REVIEW'?'Prüfen':'Kritisch'}</div><p>${esc(d.action)}</p></div><span class="badge ${d.state==='OK'?'good':d.state==='REVIEW'?'warn':'bad'}">Daten ${d.quality}%</span></div><div class="quality421"><span style="width:${d.quality}%"></span></div><div class="decision-reasons421">${d.reasons.map(r=>`<div><span>•</span><span>${esc(r)}</span></div>`).join('')}</div><div class="rule-source">Berechnet aus HPOS-Regeln, effektivem Halal-Status, Portfolio-/Kursdaten und News-Datenstatus. Keine KI-Freigabe.${last?` · Manuell aktualisiert ${esc(dateFmt(last))}`:' · Wird bei Öffnen neu berechnet.'}</div><div class="action-row"><button class="btn primary small" data-analysis-refresh421>Analyse aktualisieren</button><button class="btn small" data-analysis-ai421>Mit ChatGPT vertiefen</button></div></div><div class="section-head"><div><h2>Regeln & Signale</h2><p>Nachvollziehbare Auslöser</p></div></div><div class="stack">${v.alerts.length?v.alerts.map(x=>alertCard(x)).join(''):'<div class="card empty">Keine assetbezogenen Hinweise.</div>'}</div>`;}if(tab==='halal'){const base=tab421(v,tab),auto=(state.halalChecks||[]).filter(c=>c.assetId===v.asset.assetId&&String(c.enteredBy||'').toLowerCase()!=='user').sort((a,b)=>String(b.checkedAt).localeCompare(String(a.checkedAt)))[0];return`<div class="card verify421"><div class="top"><div><h3>Halal-Verifikation</h3><p>Automatik zuerst, manuelle Verifikation als zweite Ebene.</p></div><span class="badge ${auto?'good':'info'}">${auto?'Auto-Beleg vorhanden':'Auto nicht verbunden'}</span></div><div class="steps421"><div><small>1 · Automatische Vorprüfung</small><strong>${auto?`${esc(auto.source)} · ${esc(auto.status)}`:'Noch keine belastbare API-/Feed-Quelle'}</strong></div><div><small>2 · Manuelle Härtung</small><strong>${v.halal.status==='UNKNOWN'?'Ausstehend':'Historie vorhanden'}</strong></div></div><div class="rule-source">HPOS übernimmt keinen Halal-Status aus News oder Kursdaten. Ohne verifizierte Screening-Quelle bleibt UNKNOWN bestehen.</div><div class="action-row"><button class="btn small" data-halal-auto421>Automatik-Status</button><button class="btn primary small" data-add-halal>Manuell verifizieren</button></div></div><div style="margin-top:12px">${base}</div>`;}return tab421(v,tab);};

const detail421=renderAssetDetail;renderAssetDetail=function(){detail421();if(currentPage!=='asset')return;$('[data-analysis-refresh421]')?.addEventListener('click',async()=>{const u=ui421();u.analysisAt=u.analysisAt||{};u.analysisAt[currentAssetId]=new Date().toISOString();persist();recompute();await loadMarket421(true);renderAssetDetail();toast('Analyse aus aktuellen belegten Daten neu berechnet');});$('[data-analysis-ai421]')?.addEventListener('click',()=>openAiHandoff());$('[data-halal-auto421]')?.addEventListener('click',()=>openModal('Halal-Automatik',`<div class="stack"><div class="notice info"><strong>Noch keine belastbare Live-Screening-API verbunden.</strong><p>Musaffa, Zoya und Muslim Xchange bleiben im Quellenregister. HPOS automatisiert den Status erst, wenn eine zulässige und technisch belastbare Quelle verfügbar ist.</p></div><div class="card"><h3>Sicherheitsregel</h3><p>Fehlende Automatik wird niemals als H1 interpretiert. Konflikte werden nicht automatisch aufgelöst.</p></div></div>`));if(currentAssetTab==='overview')decoratePrice421();};
async function decoratePrice421(){await loadMarket421();if(currentPage!=='asset'||currentAssetTab!=='overview')return;const a=state.assets.find(x=>x.assetId===currentAssetId),m=marketMeta421(a);if(!m||!Number.isFinite(Number(m.regularMarketPrice)))return;const host=$('#chart42');if(!host)return;const run=()=>{const top=host.querySelector('.top>div');if(!top||top.querySelector('.price-eur421'))return;const local=Number(m.regularMarketPrice),eurPrice=Number(m.regularMarketPriceEur);const e=document.createElement('div');if(Number.isFinite(eurPrice)){e.innerHTML=`<div class="price-eur421">≈ ${fmt421(eurPrice,'EUR')}</div><div class="price-local421">${fmt421(local,m.currency)} · FX ${esc(m.fxProvider||'Referenzkurs')}</div>`;}else e.innerHTML=`<div class="price-local421">${fmt421(local,m.currency)} · EUR-Umrechnung nicht verfügbar</div>`;top.appendChild(e);};run();setTimeout(run,350);setTimeout(run,1000);}

const halal421=renderHalal;renderHalal=function(){halal421();const unknown=state.assets.filter(a=>getEffectiveHalal(state,a).status==='UNKNOWN').length,h2=state.assets.filter(a=>getEffectiveHalal(state,a).status==='H2').length,h0=state.assets.filter(a=>getEffectiveHalal(state,a).status==='H0').length;const head=$('#main .section-head');if(head){const q=document.createElement('div');q.className='card verify421';q.style.margin='0 0 12px';q.innerHTML=`<div class="top"><div><h3>Verifikationsqueue</h3><p>Automatische Belege priorisieren, manuell härten.</p></div><span class="badge info">${unknown+h2+h0} prüfen</span></div><div class="asset-badges"><span class="badge info">UNKNOWN ${unknown}</span><span class="badge warn">H2 ${h2}</span><span class="badge bad">H0 ${h0}</span></div>`;head.after(q);}renderNav();};

const strategy421=renderStrategy;renderStrategy=function(){strategy421();const main=$('#main');if(!main)return;const sec=document.createElement('div');sec.innerHTML=`<div class="section-head"><div><h2>Verwaltung</h2><p>Seltene Aufgaben, bewusst aus der Hauptnavigation entfernt</p></div></div><div class="manage421"><div class="card manage-group421"><div class="manage-title421">Daten & Sicherheit</div><button class="manage-row421" data-manage421="data"><span>⇩</span><span><strong>Daten & Backup</strong><small>Sichern, wiederherstellen, zurücksetzen</small></span><span>›</span></button><button class="manage-row421" data-manage421="diagnostics"><span>!</span><span><strong>Diagnose</strong><small>Datenqualität und technische Hinweise</small></span><span>›</span></button></div></div>`;main.appendChild(sec);$$('[data-manage421]').forEach(b=>b.onclick=()=>route(b.dataset.manage421));renderNav();};
renderMore=function(){route('strategy',false);};

css421();loadMarket421();document.title='HPOS Alpha 4.2.1 Decision UX';renderNav();route(currentPage==='more'?'strategy':currentPage,false);
})();
